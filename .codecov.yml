codecov:
  branch: master

coverage:
  status:
    project:
      default: off
      ios:
        flags: ios
      core:
        flags: core

flags:
  ios:
    paths:
      - ScheduleApp/ScheduleApp/
  core:
    paths:
      - Core/Core/

ignore:
  - "ScheduleAppTests/.*"
  - "Core/CoreTests/.*"
